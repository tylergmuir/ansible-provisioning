---
- name: upgrade packages
  ansible.builtin.dnf:
    name: "*"
    state: latest

- name: set chrony config
  ansible.builtin.copy:
    src: chrony.conf
    dest: /etc/chrony.conf
    mode: '0644'
